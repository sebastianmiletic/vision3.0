# Applications

Deployable runtime surfaces in Vision v3.0.

- `web-command`: frontend command-center UI.
- `api-gateway`: frontend-facing REST edge service.
- `geospatial-service`: geospatial cache and normalized APIs.
- `data-ingestion`: ingestion pollers and source normalization.

Deep app documentation is in `../doccumentation/02-apps/`.
